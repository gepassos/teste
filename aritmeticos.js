const [a, b, c, d] = [4, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // resto da divisao divido por 2, se for numero redondo ele retorna 0 se for 1 ele é número com sobras.

console.log(soma, subtracao, multiplicacao, divisao, modulo)


 